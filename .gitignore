# 底图 / 影像 tiles（太大，不同步）
tiles/
GEE_tile/
tiles_satellite_2025/

# QGIS 项目文件
*.qgz
*.qgs
*.qgs~

# GeoPackage 数据文件（标注真值 + 任务网格）
*.gpkg
*.geojson

# 检测结果（可重新生成；review 工件走 Dropbox/外存，不入仓）
results/

# 模型权重（太大，不同步）
checkpoints/
*.pth

# 训练数据（可重新生成）
data/coco*/
data/cls_pv_thermal/
data/cls_pv_thermal_v*/
data/cls_pv_nonpv_v*/
data/geid_temporal
data/geid_temporal/

# Negative pool: manifest + taxonomy + README tracked; preview symlinks local-only
data/negative_pool/previews/

# 建筑轮廓数据（可重新生成）
buildings.gpkg
tile_manifest.csv

# JHB 数据（太大，不同步）
johnberg/
results_joburg/

# Claude Code plugin / agent harness (local-only, not part of public repo)
AGENTS.md
CLAUDE.md
.agents/
.claude/

# 个人文档 / 研究 journal / 内部 SOP
progress.tex
docs/progress_log/
docs/runbook/

# 外部论文 / PDF
*.pdf

# Credentials / env files
.env
.env.*
*.credentials
*.secret

# Python
__pycache__/
*.pyc
*.pyo
.venv/
.venv.bak.*/
.cache/
.config/
.local/
.tmp/
*.egg-info/

# 运行日志
run_output.txt
logs/

# OS
.DS_Store
Thumbs.db

# Other agent harness local state
.codex
.hermes/

# Local cache (OSM buildings, tile masks, etc.)
cache/

# Misc data files
data/sseg_registration_geo.csv

# Annotations clean GT (派生产物，由 build script 重生)
data/annotations_channel2_clean/

# Vexcel eval sampling intermediates (final benchmark uses jhb_cbd25_*.csv only)
data/vexcel_eval_samples/vexcel_eval_grids_*.csv

# Experiment-derived figures and per-grid tables (regeneratable from run-evaluation)
docs/experiments/**/*.png
docs/experiments/**/per_*.csv
docs/experiments/**/summary.csv

# Generated visual diagnostics / local imagery comparisons
mosaics/

# Channel 2 micro working set (per-grid Vexcel mosaics + draft GT/prediction
# overlays for G0774/G0816/G0922 iteration). Curated outputs land in
# data/annotations_channel2_clean/ which IS tracked.
data/annotations_channel2_micro/

# Claude Code scheduled-task runtime lock file
.claude/scheduled_tasks.lock

# Building footprint data (downstream's responsibility per project_adoption_rate_dimension)
data/buildings/

# Imagery vintage probe artifacts (large, regeneratable from scripts/imagery/)
probes/

# Pod transfer tarballs
jhb_phaseA_*.tar.gz

# Generated git activity report
docs/git_activity_report.html

# Experiment archives: keep README/MANIFEST, drop heavy artefacts
docs/experiment-archive/**/*.gpkg
docs/experiment-archive/**/*.kml
docs/experiment-archive/**/*.parquet
docs/experiment-archive/**/_build_summary.json
