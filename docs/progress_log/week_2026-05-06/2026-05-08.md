# 工作记录 2026-05-08 (Fri)

**工作时间**: 约 3.3 小时（截至 15:16 NZST 仍在工作；按 Claude/Codex 记录合并 wall-clock，午前 4 段会话 ~16min 已并入 05-07 日报作前一晚延续，本日从 12:01 NZST 起重新计时；下午段持续到 15:16+，工时随后续工作再增长）。
**对话记录时间段**: Claude Code 12:13-15:16（含 Edge/Chrome 本地 HTTP review、HN 数据检查、RunPod pod 起停 + tar 推送、JHB 全城 grid 规划等多线并行）；Codex 12:01-15:16（4 个 rollout，覆盖 prep deliverable review、训练集/HN 检查、JHB 全城规划、并行 idea 整理）。
**证据来源**: `/home/gaosh/.claude/projects/-home-gaosh-projects-ZAsolar/**/*.jsonl`（含 `0ca2d523`, `33dd34b0`, `ab4ceea3`, `f3b280de`, `fef2d7a3`）、`/home/gaosh/.codex/sessions/2026/05/08/*.jsonl` (4 rollouts: `12-01`, `13-25`, `14-24`, `14-55`)，按 Pacific/Auckland 本地日期过滤；ZAsolar commit `b5764b87 feat(analysis): finalizer pixel-or vs per-detection ablation suite` (14:52:17 NZST)。

## 概述

本日全部用在"为下一轮 25-grid 训练 + JHB 全城推理铺路"上：上午 prep deliverables review + 16 个 split-array case 的 HTML 复核 + HN 数据集核查 → 14:52 落 commit `b5764b87`（finalizer pixel-or vs per-detection ablation suite，4 个分析脚本 +2192 行） → 起 4090 RunPod pod (213.173.98.6:36928)、推 coco_train20_val5_hn tar、用 V3-C 作为初始权重起新一轮训练 → 同时规划 JHB 全城（+其余 7 metro + CT）按 Vexcel 优先 / aerial_2023 兜底的 grid 切法 + Overture/OSM admin level 8 building footprint 拉取的下行需求。

## 工作完成

### Prep deliverables review + 16 个 split-array case 复核

- 检视上午 4 个 prep deliverables 的产出（25-grid 训练集组装、HN 选择、Stage 3B/finalizer 文档化、JHB Vexcel 行政区覆盖核对）。
- review 产出 1 个训练前建议修的问题 + 2 个风险（已确认不修）。
- 起本地 HTTP server，通过 Windows Chrome 打开 16 个 split-array case 的 HTML 复核（在 Vexcel grid 边界被切成两半的大 PV 阵列，几个属"邻 grid 各占一半"的同一装机）；逐个标注、最后 1 个保留为正确不修，剩余 15 个完成；导出。
- 关键结论："grid 切法把同一大装机切成两个" 是 grid 重新规划要解的几何问题，不是训练集干净度问题——直接进入 JHB 全城 grid 重新规划。

### Finalizer pixel-or vs per-detection ablation suite (commit b5764b87)

- 把 05-06/05-07 跨 grid 跑出来的实验代码整理成正式 analysis suite，commit `b5764b87`：
  - `scripts/analysis/finalizer_envelope_group_sweep.py` (749 行) — Stage 3 envelope-group fill `(area, density, count)` 三参数 sweep。
  - `scripts/analysis/finalizer_stage3b_postnms.py` (206 行) — Stage 3B post-NMS dedup 评估。
  - `scripts/analysis/finalizer_stage3b_sam.py` (700 行) — Stage 3B cluster→SAM mask refinement 评估。
  - `scripts/analysis/raw_hint_audit.py` (537 行) — 审计漏检 GT polygon 背后的原始 detector hint，定位"recall 失败 vs 后处理失败"边界。
- 适用 grid: G0816 / G0817 / G0925 cross-grid Channel-2 clean GT；commit message 写明"compares finalize.py --merge-mode pixel-or vs per-detection"，把 05-07 的暂定结论"pixel-or 仍是面积最优解、per-detection 必须配实例级二级过滤"封进 commit 历史。

### RunPod 4090 pod 起停 + 训练 tar 推送 + 起新一轮训练

- 先开了便宜 pod 走 prep / S3 空间核对，确认 `s3://h9uxjmkhr9/` 空间够；workspace 现状盘点找出可清理的旧 artifact。
- 关停便宜 pod，换 4090 (host 213.173.98.6 port 36928)，按规则更新 `.env` `RUNPOD_SSH_HOST/PORT` + git `pod` remote，known_hosts 重置。
- 用 runpodctl 把 `coco_train20_val5_hn` tar 直推到 `/workspace`（不解包）；解包到 `/root` 后挂载到 `/dev/shm`（沿用 RTX 推理最佳实践 IO 路径）。
- val 底图 + GT 一并传一份到 pod，方便训练完直接跑推理验证。
- 训练端确认不再依赖 geoai 封装暴露的接口（推理端已经在 V1.4 拆掉），直接从 V3-C 作为初始权重起新一轮（25-grid GT，CT aerial_2025 + JHB Vexcel + JHB 2023 aerial 兜底，HN 比例沿用 v4 + skylight/汽车/地面标线加权）。
- 训练 run 在 4090 上启动后开始监控；同时把本地新写的 finalizer ablation 脚本同步到 pod 端方便训练完直接跑。

### JHB 全城推理 grid 重新规划（前期数据需求）

- 当前 JHB 现网 grid 是按行政区直接生成的，很多 grid 的 Vexcel 影像覆盖不全；新规划目标是"先把 Vexcel 上有的 JHB 底图全量切 grid，命名沿用其它 6 个 metro Vexcel grid 切法 + 命名规则"。
- 行政区裁剪：用 OSM admin-level 8 relation（Overpass API 一次拉完，免费、可脚本化）；7 metros + CT 一起拉。
- Building footprint：用来在 grid 内只下载有建筑的 sub-tile（避开荒地 / 水体 / 高速），按 Vexcel 7 城 + CT 估算下载数据量。
- JHB 内部覆盖策略：Vexcel 覆盖区优先；行政区内 Vexcel 没覆盖但有建筑区域 → 走 aerial_2023 兜底（明知清晰度低，但建筑边界还在）。
- 启动小规模图源可用性探测：City of Joburg ArcGIS 已知是 2023 aerial，其余 metros 走 ArcGIS 探测；探测脚本/查询初步 spec 已经过 Codex 一轮整理。

### 与训练并行的旁路任务清单

- 等训练时与云端 GPU 不冲突的并行候选：JHB 全城 grid 重新切（本地脚本 + Overture/OSM 数据拉取）、ArcGIS 图源探测、SAM maskbox 在新 25-grid clean GT 上的量化基线、daily/weekly 文档同步。
- 优先级：先 JHB grid 切（解 split-array 的几何根因）→ ArcGIS 图源探测（解非 JHB metro 的图源问题）→ 同步 daily log / 周报。

## 时间块

- 12:01-13:25 NZST - prep deliverables review、HTML 16 case 复核（Edge/Chrome 本地 HTTP）、HN 数据集 `~/zasolar_data/coco/coco_train20_val5_hn/` 检查、训练前 1 个建议修的风险确认。证据：Codex rollout `2026-05-08T12-01-16`；Claude sessions `0ca2d523`, `33dd34b0`。
- 13:25-14:55 NZST - finalizer ablation suite commit `b5764b87` (14:52)、RunPod 4090 起 (213.173.98.6:36928)、coco tar 推送 + 解包到 `/dev/shm`、val 底图/GT 同步、新一轮训练从 V3-C 起跑。证据：Codex rollouts `2026-05-08T13-25-25`, `2026-05-08T14-24-07`；Claude sessions `ab4ceea3`, `f3b280de`；ZAsolar commit `b5764b87`。
- 14:55-15:16 NZST - JHB 全城 grid 重新规划 (Vexcel + aerial_2023 兜底 + Overture/OSM admin)、ArcGIS 图源探测启动、与训练并行的旁路任务清单整理；本日报 backfill 触发。证据：Codex rollout `2026-05-08T14-55-01`；Claude session `fef2d7a3`。

## Notes / uncertainty

- 截稿时（15:16 NZST）训练还在 4090 上跑，本日报后续工时（监控/调整/分析）会归到 05-08 而不是新建子日报；如果训练跨午夜则按"跨午夜延续归入前一日"处理。
- JHB 全城 grid 切法依赖 Overture / OSM admin level 8 拉取，数据量级要等具体拉完才知道（7 metros + CT 行政区面积 × building footprint 密度），先按"<10GB 估算 + 按需扩"走。
- finalizer ablation suite (b5764b87) 的脚本主要是把 05-06/05-07 离线实验的代码 finalize 成可复用 analysis tool；不直接影响生产 finalize.py 路径——生产路径仍然走 `ref_sam_maskbox` + Stage 3 不开启 envelope-group。
- 老板昨日（05-06）对工时质疑的回应：本周从今天起会按"每日工时 + 每日产出"双线 commit / 同步，方便他直接对照工作日的 git 历史看进度，不再只看周末汇总。
