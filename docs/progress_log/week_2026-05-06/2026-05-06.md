# 工作记录 2026-05-06 (Wed)

**工作时间**: 约 4.3 小时（按 Claude/Codex/Hermes 记录合并 wall-clock，分散三段：上午、午后短讨论、晚间主工程；本日属新一周第一天，凌晨 00:01-00:32 NZST 已并入 05-05 日报作为前一晚连续工作流延续，不在本日重复计时）。
**对话记录时间段**: Claude Code 09:58-11:08、12:34-12:58、21:00-23:41；Codex 10:02-10:09、21:36-23:35；Hermes 09:36-10:19（4 个会话，集中于周报核对与凌晨对话补入日报）。三个时段间为长 idle / 通勤 / 餐饮 / 晚间 RA 例会，不计入工作时长。
**证据来源**: `/home/gaosh/.claude/projects/-home-gaosh-projects-ZAsolar/**/*.jsonl`、`/home/gaosh/.codex/sessions/2026/05/06/*.jsonl`、`/home/gaosh/.hermes/state.db`（sessions `4月29日报记录排查` / `凌晨对话补入日报`），按 Pacific/Auckland 本地日期过滤；ZAsolar commit `76c6b342 Add adaptive mask shaping controls` (23:31:30 NZST)。

## 概述

完成 progress_log 周目录从"周一开始"切换到"周三开始"的结构迁移，把 05-05 凌晨工作并入前一日并清算 04-29→05-05 周工时口径；午后被老板的工时/进度质疑打断（非工程时间，但为本日明确的项目相关讨论）；晚间回到 census 模型主线，针对 V3-C 在大面板阵列上的"漏检不报"问题做 NMS IoU 放宽探针 + source-aware mutual-IoU + Stage 3 envelope-group fill 的递进实验，最终把 adaptive mask shaping controls 落到主仓 commit。

## 工作完成

### Progress log 周结构迁移 + 05-05 工时重算

- 把 `docs/progress_log/` 周目录命名规则从"周一第一天"改为"周三第一天"，对齐项目内已经形成的 Wed→Tue 节奏；现存周目录（`week_2026-04-29` 等）已经按新规则落位，本周从 `week_2026-05-06/` 起。
- 修订 05-05 日报：把 12:41-13:57 与 19:22-20:38 两段 ~76min 的长 idle 重新归类为午餐 / 晚餐休息（不计入工时），并把 05-06 凌晨 00:01-00:32 的日报核验 / 周报压缩 / Dropbox kanban 同步作为前一晚连续工作流的延续合并到 05-05；最终净工时 11.9h。
- 配套排查 04-29→05-05 周报里"36 小时"周工时的口径来源，确认旧版本是 wall-clock 求和未扣 idle，新版本压缩到 ~12h/周（与日报合计一致）。

### 子仓 public 化与 OCR 工具调研（短小杂事）

- 把 sibling repo `solar_backdating` 在 GitHub 上转 public，并核对仓库 README/AGENTS/CLAUDE 入口与主仓的一致性，方便后续向老板汇报时直接给链接。
- 调研 Zhipu GLM-OCR (0.9B) 在 4070 Laptop 上的本地部署可行性：评估 Windows 直装 vs WSL2 路径，结论是显存够，但当前还没有明确 OCR 需求，仅作为后续地址点 / 行政材料 OCR 的备选不立即上手。

### 中午 RA 工时/进度认知差讨论（非工程时间，按用户校准计入）

- 老板在 review 中提出"36 小时/周不匹配进度"质疑；本人作为 part-time RA 与另一位纯标注 RA 的分工差异、卫星图普推三周 vs 航测 census 路线的资源错配、署名/致谢预期等问题做了一次较长的对话纪录与梳理。
- 这段时间不是新代码 / 新结果产出，但属于"本项目相关的工作沟通规划"，按当前工时口径计入；晚间被叫去开会前先把技术主线推到 commit，避免被打断后丢上下文。

### 大面板阵列漏检调查 (G0817 NMS probe + source-aware mutual-IoU)

- 重新把 V3-C 在 JHB CBD Vexcel 上的主要失败模式定位为"大装机阵列里只命中 1/N 个 sub-array，剩余 19/N 全是 SAM 补标"——是 recall 失败而非碎片化，且降置信度阈值会换来 FP 爆炸（已经在过往实验中验证），不是出路。
- 在 G0817 上跑 NMS IoU 放宽探针（从默认 0.5 → 更宽松阈值），用本地 GPU；count recall 中位 +bump，但 area recall 反向下降，确认 NMS 放宽只是诊断工具，不能直接做产品后处理（IoU bump 中位 ~0.030，不足以覆盖大板几何空隙）。
- 按 GT source 拆分（V3C_TP / SAM_supp / Li[include]）做 mutual-IoU 分析，结论：source-aware 只能做诊断（推理时 GT source 不存在），cluster-aware 才能进入产品路径；mutual-IoU 是 dedup 不是 gap-fill，V3C_TP area 卡在 0.70、max area 372.4 vs 871.5 也佐证丢的是几何空隙不是重复 anchor。
- 把"V3-C recall 修复必须走 training-time（TTA / 训练数据补充）"这条 invariant 在 memory 里更新（参考 feedback_recall_recovery_constraints.md），后续不再重复在阈值/NMS 这一层折腾。

### Stage 3 envelope-group fill (G0817 单 grid)

- 在 G0817 上做 envelope-fill 后处理 sweep：以 `(area_thresh, density, count)` 三参数网格搜索；得到 `a100_d0.45_c3` (F1 0.742) 反超 SAM maskbox (0.729) +1.3pp，`a150_d0.50_c3` (0.728) 持平；conservative vs loose 选择有局部最优。
- 关键迭代：单 cluster 内的 envelope-fill 不能解决问题，必须按 envelope-group（同一屋顶上多个相邻 cluster 的整体外包络）触发，才能填回 sub-array 之间的几何空隙；写到 `core/postproc.py` 与 `finalize.py` 的 Stage 3 控制参数里，并新增 `tests/postproc/test_mask_binarization.py`、扩 `tests/postproc/test_load_config.py`、`tests/inference/test_finalize_schema.py`。
- 落 commit `76c6b342 Add adaptive mask shaping controls`：5 文件 +627/-25；Stage 3 envelope-group fill 在 G0817 上是局部最优，但还需要在第二个 grid 上验证是否过拟合 G0817——这是 05-07 的工作起点。

## 时间块

- 09:36-10:19 NZST - Hermes 4 段会话围绕 04-29 周报与 05-05 凌晨对话补入做账面核对（不算独立工时块，与 09:58-11:08 Claude/Codex 主线合并）。证据：Hermes sessions `4月29日报记录排查` / `凌晨对话补入日报`。
- 09:58-11:08 NZST - 周目录结构迁移到 Wed→Tue、05-05 日报工时重算、`solar_backdating` 子仓 public 化、Zhipu GLM-OCR 笔记本部署可行性调研。证据：Claude sessions `b211cbe6`, `d67b865b`, `f8ccf17e`；Codex rollout `2026-05-06T10-02-52`。
- 12:34-12:58 NZST - 老板工时/进度质疑相关的 RA 沟通梳理（项目相关讨论，无代码产出）。证据：Claude session `021d2fcb`。
- 21:00-23:41 NZST - 回到 census 主线：大面板阵列 recall 调查 → G0817 NMS IoU 放宽探针 → source-aware mutual-IoU 分析 → Stage 3 envelope-group fill sweep → 落 commit `76c6b342`。证据：Claude sessions `0106c4e4`, `113761a3`, `32839ccf`, `9b2d7719`, `fe75d3ec`；Codex rollouts `2026-05-06T21-36-08`, `2026-05-06T21-53-59`；ZAsolar commit `76c6b342` 23:31:30 NZST。

## Notes / uncertainty

- 工时偏低（4.3h）符合本日实际：上午被周报口径排查 + 杂事占用，下午晚饭前一直被工时质疑 / RA 情绪打断没动技术主线，晚饭后开会，开会后才开始集中跑实验。没有刻意凑工时，工时数据如实反映"被打断的一天"。
- 12:34-12:58 这段算不算工时存在主观判断：按"本项目相关的工作沟通"计入（与 RA 协作 / 优先级排序 / 老板汇报口径等等），不算"为消耗工时而开的闲聊"。如老板对此口径有异议可以从总工时里再扣 0.4h（→ 3.9h）。
- Stage 3 envelope-group fill 在 G0817 上是局部最优，但 SAM_supp median IoU 在 conservative trigger 下并未明显退化，因此 conservative vs loose 的选择需要 G0925 + 第二个 grid 的 cross-grid sweep 复核——这是 05-07 的明确入口。
- 凌晨 00:01-00:32 NZST 的工作（日报核验 + 周报压缩 + Dropbox kanban 同步）按"跨午夜延续归入前一日"口径并入 05-05，本日不重复计时。
