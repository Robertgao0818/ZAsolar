# 工作记录 2026-05-07 (Thu)

**工作时间**: 约 1.5 小时（按 Claude/Codex 记录合并 wall-clock，分两段：上午看昨晚跑出来的 cross-grid sweep 结果 + 晚间 Stage 3B + 下一轮训练规划。本日是低产出日，白天主要做线下事务/休息，没有人为凑工时。05-08 凌晨 00:01-00:17 NZST 16min 作为 05-07 晚间工作流的延续并入本日报）。
**对话记录时间段**: Claude Code 23:53-00:01 + 跨午夜 00:01-00:17（05-08）；Codex 08:53-09:08、22:58-23:42。两段间长 idle（白天）不计工时。
**证据来源**: `/home/gaosh/.claude/projects/-home-gaosh-projects-ZAsolar/**/*.jsonl`（含 `3701ff38`, `733b1179`, `eaabf9f0` 等跨午夜会话）、`/home/gaosh/.codex/sessions/2026/05/06/rollout-2026-05-06T21-53-59-*.jsonl`（跨日运行至 05-07 晚），按 Pacific/Auckland 本地日期过滤。本日无新增 commit（实验结果作为 05-08 commit `b5764b87` 的输入）。

## 概述

把 05-06 commit 出去的 Stage 3 envelope-group fill 在 G0925 + G0816 上做 cross-grid sweep 复核，发现 G0817 envelope-group trigger 在 cross-grid 上 area_F1 反而落后于 old pixel_or（过拟合 G0817），SAM maskbox 是 area_F1 最稳定的第二名；继续做 Stage 3B（cluster/envelope box → SAM mask refinement）的 cross-grid 探索，结合 finalizer pixel-or vs per-detection 的对比讨论"per-detection 提 polygon F1 但 area F1 -12.9pp"的 trade-off；最后规划 05-08 起的下一轮训练（25-grid GT + CT aerial_2025 + JHB Vexcel，丢 V4.2 的 JHB 2023 aerial）。

## 工作完成

### G0817/G0925/G0816 cross-grid envelope-group sweep

- 沿用 05-06 在 G0817 上的 `(area_thresh, density, count)` sweep 网格，把同一 finalizer 控制参数应用到 G0925、G0816 两个 cross-grid 复核 grid，跑出 per-grid CSV + summary.md + cross-grid summary。
- 结论：Stage 3 envelope-group fill **在 G0817 上是局部最优，但在 cross-grid 上 area_F1 反而被 old `pixel_or` 反超**——属于 G0817-specific 过拟合，envelope-group trigger 不能直接进生产路径；SAM maskbox 仍然是 area_F1 最稳定的第二名。
- memory 同步：`project_envelope_group_fill_overfit.md` / `feedback_recall_recovery_constraints.md` 中的口径用 cross-grid 数据补强。

### Stage 3B (cluster/envelope box → SAM mask refinement)

- 在 cross-grid 三个 grid (G0817 / G0925 / G0816) 上做 Stage 3B 探索：把 cluster envelope 作为 box prompt 喂给 SAM 拿 refined mask，再叠 post-NMS dedup。
- 结果：Stage 3B 在 area_F1 上跟不上 `ref_sam_maskbox`，但 hybrid+NMS 是 SAM_supp polygon-quality 的新 cross-grid 领先；ref_sam_maskbox 仍保持生产路径地位。
- 与 finalizer 模式对照讨论：pixel-or 的几何缺口（"对一整个大太阳能板，检测头不一定对整块都给高分，可能给了一片片"）vs per-detection 的 polygon F1 优势但 area F1 -12.9pp 的 trade-off；本日得出"pixel-or 仍是当前面积聚合最优解、per-detection 必须配实例级二级过滤才能用"的暂定结论（详见 05-08 ablation suite commit）。

### 下一轮训练规划

- 25-grid clean GT 作为新一轮训练的核心训练集；丢弃 V4.2 加进来的 2023 JHB aerial（清晰度低 + GEID/Vexcel 不对齐），专注 CT aerial_2025 + JHB Vexcel 2024。
- 训练集组成初步定为 20 JHB + 20 CT；CT 选 grid 时倾向选大面板多的、不要全是别墅区类型；JHB 没被 Vexcel 覆盖的 grid 走 aerial_2023 兜底。
- HN 策略沿用 v4 思路（focus on delete 部分），但本轮加大 skylight / 汽车 / 地面标线类负样本比例。
- 大板"是否聚合然后整体喂"作为待评估问题挂起，下个工作日做 prep deliverables 时给具体方案。

## 时间块

- 08:53-09:08 NZST - 看 05-06 晚间在 G0925 + G0816 上跑的 envelope-group cross-grid sweep 结果，确认 G0817 trigger 是局部最优而不是 cross-grid 最优。证据：Codex rollout `2026-05-06T21-53-59`（跨日延续）。
- 22:58-23:58 NZST - Stage 3B (cluster→SAM) 探索：三 grid 复核 finalizer pixel-or vs per-detection trade-off、确认 hybrid+NMS 是新 SAM_supp polygon-quality 领先；规划下一轮 25-grid 训练 (CT aerial_2025 + JHB Vexcel + drop 2023 JHB)，初定 20+20 grid 比例和 HN 类型分布。证据：Claude sessions `3701ff38`, `733b1179`, `eaabf9f0`；Codex rollout `2026-05-06T21-53-59` 尾段。
- 00:01-00:17 NZST 2026-05-08 - 跨午夜延续：把 23:58 之前的训练规划落到具体 Vexcel 行政区覆盖检查 + grid 选择初稿（20 JHB + 20 CT），未覆盖区域走 aerial_2023 兜底；对应 RA review prep 的前置文件夹/路径定位。证据：Claude sessions `733b1179`, `eaabf9f0` 尾段。

## Notes / uncertainty

- 本日是项目低产出日，白天没有任何 agent / git 活动；不刻意凑工时，1.5h 实事求是。
- "Stage 3B exploration complete"的结论从 05-07 23:31 codex output 来，但完整 ablation suite 的代码 commit 在 05-08 14:52 `b5764b87`；05-07 实际产出是结论 + memory + 05-08 工作清单，没有独立 commit。
- 25-grid 训练规划尚未涉及 large-array aggregation 的具体策略（"大板聚合然后整体喂"），这部分 05-08 prep 阶段会给方案；若聚合后训练效果不好，HN 策略可能要再迭代。
