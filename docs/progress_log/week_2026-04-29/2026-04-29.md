# 工作记录 2026-04-29 (Wed)

**工作时间**: 约 9.0 小时（按 Claude/Codex 对话记录校准；合并 cluster 后 00:00-00:55 + 12:04-12:44 + 18:28-23:50 ≈ 6:57，另加 18:28 前人工标注约 2.0 h）
**对话记录时间段**: Claude 00:00-00:55、12:04-12:44、18:28-23:50；Codex 19:01-19:12、19:25-20:25、22:54-22:59、23:26-23:33。晚间 Claude/Codex 多段并行，按合并后的本地时间段计入，不重复累加；05:48-05:51 仅为隔夜任务通知检查，未计入工时。
**证据来源**: `/home/gaosh/.claude/projects/-home-gaosh-projects-ZAsolar/**/*.jsonl`、`/home/gaosh/.codex/sessions/2026/04/29/*.jsonl` 与 `/home/gaosh/.claude/history.jsonl`，按 Pacific/Auckland 本地日期过滤；18:28 前人工标注时间按用户补充记录计入。

## 概述

今天的主线是把 **Vexcel 底图验证、RunPod 推理流程、geoai 解耦方案、以及 JHB CBD V3-C/Vexcel 人工复核工具** 串起来：先补齐 Vexcel task grid 与 60-grid 抽样底图的本地/Dropbox资产定位，再在 RunPod 上准备 JHB CBD 25-grid Vexcel 推理环境，同时围绕“去掉 opengeoai 黑箱封装”形成新的三段式 pipeline 设计，晚间进一步搭起 CH1 分层 precision review 工具并修复 Vexcel 3857 底图显示、原生分辨率显示和 SAM2 重切功能。

## 工作完成

- 继续昨晚的日志与自动监控收尾：校准 2026-04-28 日报的跨午夜工时，并监控 GEID probe / RunPod 相关后台任务状态，确认部分 S3 PUT/ACL 尝试失败原因。
- 找回并梳理 Vexcel 任务资产：确认六城全量 `data/vexcel_task_grids/`、60-grid OSM stratified seed=42 抽样表与 `tiles/vexcel_eval_60_osm_stratified_seed42_2x2/` 底图包的位置、结构、README、manifest 与各城市 tiles 体量。
- 在晚间 agent 记录开始前完成约 2 小时人工标注/审图工作，为后续 JHB CBD Vexcel/SAM 与 Li GT 对齐排查和 CH1 precision review 做输入准备。
- 同步 RA_Solar Dropbox：将 `data/annotations/` 与 `docs/progress_log/` 镜像到 Dropbox，处理 Dropbox 瞬时锁导致的 rsync permission error；随后按要求在 `Dropbox/RA_Solar/` 下补建/同步 `vexcel_task_grids` 资产。
- 在 RunPod 上准备 JHB CBD 25-grid Vexcel 推理：更新 SSH / `.env`，检查 `/workspace` 与 S3 volume，确认 S3 上只有 G0816 dry-run 而 25-grid 包需重新/本地 staging；把 task grid、JHB grid、Vexcel tiles、annotation 资产补齐到 pod，并将 25-grid Vexcel tiles stage 到 `/root` / `/dev/shm` 类高速路径。
- 排查 Vexcel dry-run 脚本：发现 `detect_and_evaluate.py` 不支持旧脚本里的 `--tiles-dir` / `--gt-path`，需要走 `--imagery-layer` 与 region registry；在 `configs/datasets/regions.yaml` 中补注册 `vexcel_2024` imagery layer，确认 dry-run 的失败不是 task grid 缺失本身。
- 梳理并评审去 opengeoai 的新 pipeline 方案：并行分析当前 `geoai` 依赖面，确认主要只有 `SolarPanelDetector.generate_masks()`、`orthogonalize()`、`add_geometric_properties()`；明确新路线保留 Mask R-CNN 权重和 mask head，采用单进程大 batch + DataLoader workers，SAM 作为 offline refine stage，`detect_direct.py` 新入口与旧 `detect_and_evaluate.py` 冻结并行，输出 raw artifact + 兼容 gpkg，post-processing 独立为 `finalize.py`。
- 对 JHB CBD G0922 Vexcel/SAM 标注与 Li GT 做偏移排查：在 WSL `/mnt/c` 出现 I/O error 的情况下改用 repo/`~/zasolar_data` 内已有数据，检查预测、GT、Vexcel mosaic、GEID mosaic 与 CRS/bounds；发现需要特别处理 Vexcel EPSG:3857 tile bounds 与 reproject 后 pred 的 EPSG:4326 对齐。
- 搭建 CH1 precision review 工具链：为 25-grid V3-C on Vexcel sample 准备分层抽样 review server，统计约 667 个 V3-C predictions 待审，决策写入 `results/johannesburg/v3c_vexcel_2024_ch1_sample/<grid>/detection_review_decisions.csv`，并预留 `compute_ch1_precision.py` 做 Horvitz-Thompson 加权 precision、per-stratum/per-grid 与 Wilson CI。
- 修复 `scripts/annotations/review_detections.py` 的 Vexcel 底图/overlay 问题：将 `_get_tile_meta` 的 tile bounds 转为 EPSG:4326 用于 pred matching，同时保留 native CRS transform 用于 pixel coordinate；修复 SAM checkpoint stat 在 `/mnt/c` I/O error 时导致 server crash 的问题。
- 根据实际审图体验继续修复 review UI：撤回不合适的 2000px 降采样，恢复 Vexcel 原生 7520×7503 px / 12 MB JPEG 底图，修正 polygon 像素坐标在原生图上的落点；最后恢复 SAM2 重切能力，从旧 venv 补回 `sam2` 包，下载/验证 `sam2.1_hiera_large.pt`，补装 `iopath/portalocker`，确认 `/api/sam_status` 返回 `available: true`。

## 时间块

- 00:00-00:55 NZST - 收尾并校准 4月28日日报/工时记录；监控 GEID probe 与 RunPod/S3 后台任务，记录 S3 ACL/PUT 失败情况。
- 12:04-12:44 NZST - 找回 Vexcel task grids 与 60-grid 抽样底图，核对 manifests/README/城市分包，并同步 RA_Solar Dropbox（annotations、progress_log、vexcel_task_grids）。
- 16:28-18:28 NZST - 人工标注/审图约 2 小时；补充检查 Vexcel/SAM 相关标注输入，为晚间 G0922 偏移排查和 CH1 precision review 做准备。
- 18:28-19:35 NZST - 处理 WSL/Claude 快速进入项目的工作流问题；用 Codex/Claude 评审拆除 opengeoai 封装的初步 plan，形成 detector + optional SAM + postproc 的迁移方向。
- 19:36-20:13 NZST - 连接 RunPod，检查 `/workspace`、S3 volume 与 Vexcel 25-grid 包；补齐 task grids、JHB annotations、Vexcel tiles staging，排查 G0816 dry-run 脚本参数与 region registry 问题。
- 20:13-20:55 NZST - 通过 codebase exploration + 逐项问答确定新 pipeline 架构：保留 Mask R-CNN，单进程大 batch + workers，SAM offline，raw artifact + compatible gpkg，`detect_direct.py` 新入口，`finalize.py` 独立 postproc，CT+JHB parity 验证。
- 20:55-22:25 NZST - 将上述设计扩展为可执行实施计划/评审意见，并继续检查 RunPod Vexcel 推理、25-grid sample 和下游 precision/review 口径。
- 22:26-23:50 NZST - 排查 G0922 Vexcel/SAM 标注与 Li GT 的系统偏移；修复 review server 的 CRS、底图原生分辨率、polygon overlay 与 SAM2 checkpoint/dependency 问题，最终让 review server 与 SAM status 正常可用。

## Notes / uncertainty

- 工时主要基于 Claude/Codex 事件聚类；晚间 Claude、Codex 与 subagent 重叠部分已合并，未把并行 agent runtime 重复累加。18:28 前 2 小时人工标注为用户补充的离线工作时间。
- 05:48-05:51 仅为隔夜任务通知/失败状态检查，已从工时中去掉。
- 18:09 左右有旧后台任务被 killed 的通知，但不一定代表 agent 记录内人工工作开始；agent 记录内晚间主工作块按更保守的 18:28 开始计入。
- 23:50 之后若继续人工审图或调试，需要在后续日报中另行追加；本日报截至解析时点约 23:50 NZST。
