# 工作记录 2026-05-03 (Sun)

**工作时间**: 约 5.5 小时（按 ZAsolar / JHB annotation 相关 Claude/Codex/Hermes 记录与用户补充的离线标注时间校准）
**对话记录时间段**: 17:46-20:36、21:48-23:11 NZST；20:36-21:48 为离线 QGIS / Review GUI 标注与检查，按用户补充计入。
**证据来源**: `/home/gaosh/.claude/projects/-home-gaosh-projects-ZAsolar/**/*.jsonl`、`/home/gaosh/.codex/sessions/2026/05/03/*.jsonl`、`/home/gaosh/.hermes/state.db`。

## 概述

今天主线是 **JHB CBD / Vexcel 大阵列 recall 问题、geoai-parity 验证、G0816 后处理 rescue，以及 Li GT + review/SAM clean GT 口径整理**。结论是：后处理可以救回一部分大面积低置信候选，但无法解决 raw proposal 阶段缺失；真正让模型学会 JHB/Vexcel 工商业大阵列，需要把全量 review + SAM FN 补标 + 少量确认正确的 Li 补充标注整理为 clean GT 并重新训练。

## 工作完成

- 重新梳理 ZAsolar 当前阶段进展，讨论接下来是继续标注，还是转向其他 TODO。
- 确认 JHB CBD 25-grid V3-C on Vexcel 推理已经完成，不应继续作为未完成 TODO 保留。
- 启动新一轮 JHB CBD / Vexcel review 与问题分析，重点检查大面积太阳能板阵列的检测失败模式。
- 分析 JHB CBD 25-grid V3-C Vexcel review 结果，尤其关注大面积 solar arrays：
  - 模型经常只检测到大阵列的一小块；
  - 剩余大面积区域需要通过 SAM 补标形成多个 FN polygon；
  - 说明当前问题主要是大阵列 recall / area recall 不足，而不是单纯 polygonization 损失。
- 使用 Codex 对近期 JHB CBD 25-grid V3-C Vexcel review 结果做更系统分析：
  - 定位源推理结果、CH1 review sample、recall audit 和 canonical review GT；
  - 对 SAM FN polygons 做空间聚类，避免把一个大阵列被切成很多小块后误读为多个独立目标；
  - 对比 SAM FN 分块面积、附近模型预测面积和大阵列整体面积；
  - 确认单次 SAM prompt 面积往往只是一个小分块，不能代表真实 FN 大阵列面积。
- 试验大阵列后处理救回方案：
  - 基于 existing raw vectors / masks 做只读 re-postproc，不重新跑模型；
  - 测试单块 raw 大候选 rescue；
  - 测试 raw cluster union rescue；
  - 结论是后处理能救回一部分 large FN，但单靠后处理收益有限，无法解决 raw 阶段完全没有可靠 proposal 的区域。
- 对比自写 direct detection pipeline 与 `geoai.SolarPanelDetector`：
  - 确认当前 direct 路径并不是简单“拆掉 geoai wrapper”后的等价实现；
  - 差异主要来自 sliding window、mask threshold、confidence 定义、面积过滤时机等。
- 规划并实现 `geoai-parity` 模式：
  - 增加 geoai-style sliding window origin / near-edge chip 规则；
  - 修正 mask threshold / binary mask 保存，使 direct parity 在单 tile 上与当前 geoai 输出逐像素一致；
  - 明确旧 cached result 与当前 geoai/direct parity 的差异主要来自旧环境、旧代码版本或旧 filtering 口径。
- 本地用 G0816 做 V3-C / Vexcel / geoai-parity 实验：
  - 完整跑一个 JHB CBD grid；
  - 对比新 parity 输出、旧 cached detection、review GT 和 SAM FN；
  - 发现命中的 polygon IoU 质量并不差，核心问题是大量 GT 没被最终结果覆盖；
  - 进一步确认 recall / area recall 低主要来自大阵列只被局部检测到。
- 针对 G0816 做 postproc rescue sweep：
  - 只放松大面积候选 confidence tier 能提升一部分 recall；
  - 但即使去掉 confidence filter，仍有一部分大阵列面积在 raw 阶段没有可靠覆盖；
  - 因此后处理只能作为短期补救，不能替代训练数据补充。
- 讨论大阵列检测能力的根本改进方向：
  - 需要把完整 review + SAM FN 补标结果纳入训练；
  - 对大阵列不能只看 polygon count，需要看 coverage / area recall；
  - 后续训练应重点补充 JHB / Vexcel 工商业大阵列样本和 hard negatives。
- 继续下一批标注 / review。
- 观察到离开 CBD 商业区后，模型在住宅区表现重新接近 Cape Town 场景：
  - 说明当前模型对住宅小型安装仍有较好迁移能力；
  - 主要短板集中在 CBD / 工商业大阵列；
  - 但最终仍需要一个能覆盖所有 Vexcel 底图类型的通用模型。
- 21:48 后继续进行 Li GT 对照和 clean GT 整理：
  - 从 Li 的 `G0774(349)new.gpkg` 中导入 / 对比 G0774、G0816、G0922 的穷尽标注；
  - 生成对比用 GPKG 和底图 mosaic，便于在 QGIS 中直接检查；
  - 用 coverage 视角重新审查 review + SAM 补标结果与 Li GT 的差异；
  - 根据 QGIS 目视检查修正部分 Li GT。
- 对部分 Li 标注逐项判断是否进入 clean GT：
  - 确认部分 Li 标注是错标或太小，不应进入 clean GT；
  - 确认部分遗漏标注可作为补充加入当前 review + SAM 结果；
  - 对 G0816 / G0922 等 micro grid 做了逐项核对。
- 明确 clean GT 的维护原则：
  - 不单独维护一个“Li GT 错误 CSV”作为核心数据；
  - clean GT 应直接由“我们的 review 结果 + SAM 补标 + Li GT 中确认正确的补充标注”生成；
  - 因为 review 时已经同步做了接近穷尽标注的 SAM 补标，所以 review corpus 本身应作为 GT 主体。
- 统计 JHB CBD Vexcel 25-grid review 工作量：
  - review 模型预测：1003 个；
  - canonical SAM 补标：146 个；
  - SAM 补标 + correct polygon predictions：808 个。
- 基于图像继续辨别疑似 FP / hard negative：
  - 识别出部分大面积浅色 roof structures / skylight / canopy 不应标为 PV；
  - 进一步说明不能简单全局降低 confidence，否则会引入大量大面积 FP。

## 产出 / 进展

- 明确了 JHB CBD / Vexcel 当前模型失败的核心问题：大阵列 area recall 不足，而不是单纯后处理或 polygonization 问题。
- 完成了对 SAM FN 分块和大阵列面积问题的聚类分析。
- 验证了后处理 rescue 可救回部分大阵列，但不能从根本上解决 raw proposal 缺失。
- 推进并验证了 `geoai-parity` 模式，明确 direct pipeline 与 geoai 的关键差异。
- 对 G0816 做了 review GT、SAM FN、parity detection、postproc sweep 的系统对照。
- 明确下一步训练数据方向：用全量 review + SAM FN 补标 + 少量确认正确的 Li 补充标注生成 clean GT。
- 对 G0774 / G0816 / G0922 的 Li GT 与 review 结果做了初步对照和修正。
- 明确后续评估应加入 coverage / area recall，不能只依赖 polygon-level count。
- 完成 JHB CBD 25-grid review / SAM / correct prediction 数量核算，为 4 月工作量统计和后续日报汇总提供依据。

## 时间块

- 17:46-18:39 NZST - 梳理项目进展，分析 JHB CBD V3-C/Vexcel review、大阵列 FN、SAM FN 聚类与 large-array rescue postproc。
- 18:53-20:36 NZST - 对比 direct detection 与 geoai，规划并实现 geoai-parity；本地跑 G0816，修复 sliding-window / mask parity，分析 G0816 recall 与 postproc rescue。
- 20:36-21:48 NZST - 离线 QGIS / Review GUI 标注与检查。
- 21:48-23:11 NZST - Li GT 对照、clean GT 口径讨论、补充标注筛选、JHB CBD 25-grid review/SAM/correct prediction 统计与日报整理。

## Notes / uncertainty

- 工时按用户补充口径记 5.5 小时；agent 可见 ZAsolar 活跃时段约 4.2 小时，20:36-21:48 离线标注 / QGIS 检查计入。
- Claude 与 Codex 并行段按合并后的本地时间段计入，不重复累加 agent runtime。
